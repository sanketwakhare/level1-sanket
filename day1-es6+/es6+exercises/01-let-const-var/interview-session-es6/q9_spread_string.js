/* Spread operator on string */

let a = [...
    'apple'
];

console.log(a);
// spread can be applied on all iterable object.
// As string is also iterable, the spread operator works here too

// output -> [ 'a', 'p', 'p', 'l', 'e' ]